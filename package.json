{
  "name": "pk-laser-ai-os",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check public/app.js && find functions -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
